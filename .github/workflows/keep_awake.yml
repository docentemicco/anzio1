name: Keep Streamlit Awake

on:
  schedule:
    # Esegue ogni 6 ore
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -s -I "https://anzio1-kob3gsp7wbd2ajsmbpgadk.streamlit.app/" | head -n 1
